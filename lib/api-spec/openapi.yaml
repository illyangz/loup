openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Loup — household services marketplace API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: home
    description: Home screen summary
  - name: catalog
    description: Categories and providers
  - name: bookings
    description: Booking engine, tracking, and chat
  - name: household
    description: Household members and activity
  - name: billing
    description: Consolidated bill and payments
  - name: pack
    description: Household thread and service requests
  - name: push
    description: Web Push notification subscriptions
  - name: openai
    description: OpenAI AI chat and benefit optimisation
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /home/summary:
    get:
      operationId: getHomeSummary
      tags: [home]
      summary: Home screen summary for the current member
      responses:
        "200":
          description: Summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HomeSummary"
  /categories:
    get:
      operationId: listCategories
      tags: [catalog]
      summary: List service categories
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
  /providers:
    get:
      operationId: listProviders
      tags: [catalog]
      summary: List providers with optional filters
      parameters:
        - name: category
          in: query
          required: false
          schema:
            type: string
          description: Filter by category slug
        - name: search
          in: query
          required: false
          schema:
            type: string
          description: Free-text search over provider name and tagline
        - name: availableNow
          in: query
          required: false
          schema:
            type: boolean
          description: Only providers available right now
      responses:
        "200":
          description: Providers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Provider"
  /providers/{id}:
    get:
      operationId: getProvider
      tags: [catalog]
      summary: Provider profile with services
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Provider detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /providers/{id}/reviews:
    get:
      operationId: listProviderReviews
      tags: [catalog]
      summary: Reviews for a provider
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Review"
  /household:
    get:
      operationId: getHousehold
      tags: [household]
      summary: Household profile with members
      responses:
        "200":
          description: Household
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Household"
  /household/activity:
    get:
      operationId: listHouseholdActivity
      tags: [household]
      summary: Recent household activity feed
      responses:
        "200":
          description: Activity items, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityItem"
  /addresses:
    get:
      operationId: listAddresses
      tags: [household]
      summary: Saved household addresses
      responses:
        "200":
          description: Addresses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Address"
  /bookings:
    get:
      operationId: listBookings
      tags: [bookings]
      summary: List household bookings
      parameters:
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [active, upcoming, past, all]
          description: Filter bookings by lifecycle scope (default all)
      responses:
        "200":
          description: Bookings, soonest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"
    post:
      operationId: createBooking
      tags: [bookings]
      summary: Create a booking
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookingInput"
      responses:
        "201":
          description: Created booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /bookings/{id}:
    get:
      operationId: getBooking
      tags: [bookings]
      summary: Booking detail with status timeline
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Booking detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookingDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
    patch:
      operationId: updateBooking
      tags: [bookings]
      summary: Reschedule or cancel a booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookingUpdate"
      responses:
        "200":
          description: Updated booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "400":
          description: Invalid update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /bookings/{id}/advance:
    post:
      operationId: advanceBooking
      tags: [bookings]
      summary: Advance a live booking to its next status (demo control)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Booking after advancing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookingDetail"
        "400":
          description: Booking cannot advance further
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /bookings/{id}/messages:
    get:
      operationId: listBookingMessages
      tags: [bookings]
      summary: Booking-scoped chat messages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Messages, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendBookingMessage
      tags: [bookings]
      summary: Send a chat message on a booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Created message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "404":
          description: Booking not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /reviews:
    post:
      operationId: createReview
      tags: [catalog]
      summary: Rate and review a completed booking
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewInput"
      responses:
        "201":
          description: Created review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Review"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /pack/messages:
    get:
      operationId: listPackMessages
      tags: [pack]
      summary: Household thread messages, oldest first (marks thread read)
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PackMessage"
    post:
      operationId: sendPackMessage
      tags: [pack]
      summary: Post a message to the household thread
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PackMessageInput"
      responses:
        "201":
          description: Created message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PackMessage"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /pack/requests:
    get:
      operationId: listServiceRequests
      tags: [pack]
      summary: Service requests from household members, newest first
      responses:
        "200":
          description: Requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRequest"
    post:
      operationId: createServiceRequest
      tags: [pack]
      summary: Request a service for head-of-household approval
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceRequestInput"
      responses:
        "201":
          description: Created request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /pack/requests/{id}/approve:
    post:
      operationId: approveServiceRequest
      tags: [pack]
      summary: Approve a pending request and create the booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Request after approval, with the created booking id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "400":
          description: Request is not pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /pack/requests/{id}/decline:
    post:
      operationId: declineServiceRequest
      tags: [pack]
      summary: Decline a pending request
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Request after declining
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "400":
          description: Request is not pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /push/public-key:
    get:
      operationId: getPushPublicKey
      tags: [push]
      summary: VAPID public key for Web Push subscription
      responses:
        "200":
          description: Public key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushPublicKey"
  /push/subscriptions:
    post:
      operationId: subscribePush
      tags: [push]
      summary: Register a Web Push subscription for the current member
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushSubscriptionInput"
      responses:
        "201":
          description: Subscription saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /push/subscriptions/remove:
    post:
      operationId: unsubscribePush
      tags: [push]
      summary: Remove a Web Push subscription by endpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushUnsubscribeInput"
      responses:
        "200":
          description: Subscription removed (or was not present)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /billing/statement:
    get:
      operationId: getBillingStatement
      tags: [billing]
      summary: Current consolidated household bill
      responses:
        "200":
          description: Current statement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillStatement"
  /billing/methods:
    get:
      operationId: listPaymentMethods
      tags: [billing]
      summary: Household payment methods
      responses:
        "200":
          description: Payment methods
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaymentMethod"
  /billing/pay:
    post:
      operationId: payBillingStatement
      tags: [billing]
      summary: Settle the current consolidated bill
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentInput"
      responses:
        "200":
          description: Statement after payment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillStatement"
        "400":
          description: Nothing to pay or invalid method
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /billing/history:
    get:
      operationId: listBillingHistory
      tags: [billing]
      summary: Past consolidated statements
      responses:
        "200":
          description: Statement summaries, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BillStatementSummary"
  /v1/demo/roles:
    get:
      operationId: listDemoRoles
      tags: [platform]
      summary: Available simulated demo roles
      responses:
        "200":
          description: Demo role choices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DemoRole"
  /v1/employee/overview:
    get:
      operationId: getEmployeeOverview
      tags: [platform]
      summary: Employee-facing allowance and life administration overview
      responses:
        "200":
          description: Employee overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeOverview"
  /v1/employer/overview:
    get:
      operationId: getEmployerOverview
      tags: [platform]
      summary: Aggregate employer benefit overview
      responses:
        "200":
          description: Employer overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployerOverview"
  /v1/employer/employees:
    get:
      operationId: listEmployerEmployees
      tags: [platform]
      summary: Employer-safe employee roster
      responses:
        "200":
          description: Employee roster
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployerEmployee"
    post:
      operationId: importEmployerEmployees
      tags: [platform]
      summary: Simulate a CSV employee roster import
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeImportInput"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
  /v1/employer/utilization:
    get:
      operationId: getEmployerUtilization
      tags: [platform]
      summary: Aggregate employer utilization metrics
      responses:
        "200":
          description: Aggregate utilization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UtilizationReport"
  /v1/employer/integrations:
    get:
      operationId: getEmployerIntegrations
      tags: [platform]
      summary: Simulated SSO and embedded widget integration details
      responses:
        "200":
          description: Integration details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntegrationDetails"
  /v1/vendor/today:
    get:
      operationId: getVendorToday
      tags: [platform]
      summary: Vendor operational day view
      responses:
        "200":
          description: Vendor day view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorToday"
  /v1/vendor/forecast:
    get:
      operationId: getVendorForecast
      tags: [platform]
      summary: Simulated vendor demand forecast
      responses:
        "200":
          description: Demand forecast
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ForecastReport"
  /v1/vendor/performance:
    get:
      operationId: getVendorPerformance
      tags: [platform]
      summary: Vendor-owned performance metrics
      responses:
        "200":
          description: Vendor performance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorPerformance"
  /v1/operations/overview:
    get:
      operationId: getOperationsOverview
      tags: [platform]
      summary: Operations control tower overview
      responses:
        "200":
          description: Operations overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationsOverview"
  /v1/operations/service-fit:
    get:
      operationId: listServiceFitEvaluations
      tags: [platform]
      summary: Internal service-fit evaluation
      responses:
        "200":
          description: Service-fit evaluations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceFitEvaluation"
  /v1/operations/audit:
    get:
      operationId: listAuditEvents
      tags: [platform]
      summary: Immutable operational audit events
      responses:
        "200":
          description: Audit events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditEvent"
  /openai/conversations:
    get:
      operationId: listOpenaiConversations
      tags: [openai]
      summary: List all conversations
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OpenaiConversation"
    post:
      operationId: createOpenaiConversation
      tags: [openai]
      summary: Create a new conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenaiConversationInput"
      responses:
        "201":
          description: Created conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiConversation"
  /openai/conversations/{id}:
    get:
      operationId: getOpenaiConversation
      tags: [openai]
      summary: Get conversation with messages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Conversation with messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiConversationWithMessages"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
  /openai/conversations/{id}/messages:
    get:
      operationId: listOpenaiMessages
      tags: [openai]
      summary: List messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OpenaiMessage"
    post:
      operationId: sendOpenaiMessage
      tags: [openai]
      summary: Send a message and receive a streaming response
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenaiMessageInput"
      responses:
        "200":
          description: SSE stream of assistant text chunks
          content:
            text/event-stream: {}
components:
  schemas:
    DemoRole:
      type: object
      required: [role, label, description, href]
      properties:
        role:
          type: string
          enum: [employee, employer, vendor, operations]
        label:
          type: string
        description:
          type: string
        href:
          type: string
    EmployeeOverview:
      type: object
      required:
        - employeeName
        - employerName
        - allowance
        - upcomingBooking
        - metrics
        - activeCategories
        - routines
      properties:
        employeeName: { type: string }
        employerName: { type: string }
        allowance:
          $ref: "#/components/schemas/AllowanceSummary"
        upcomingBooking:
          oneOf:
            - $ref: "#/components/schemas/Booking"
            - type: "null"
        metrics:
          $ref: "#/components/schemas/EmployeeMetrics"
        activeCategories:
          type: array
          items:
            $ref: "#/components/schemas/ActiveService"
        routines:
          type: array
          items:
            $ref: "#/components/schemas/Routine"
    AllowanceSummary:
      type: object
      required: [authorized, reserved, redeemed, available, renewalDate, expiration]
      properties:
        authorized: { type: number }
        reserved: { type: number }
        redeemed: { type: number }
        available: { type: number }
        renewalDate: { type: string }
        expiration: { type: string }
    EmployeeMetrics:
      type: object
      required: [employerSupport, corporateSavings, servicesCompleted, estimatedTimeSavedMinutes, householdAllocations]
      properties:
        employerSupport: { type: number }
        corporateSavings: { type: number }
        servicesCompleted: { type: integer }
        estimatedTimeSavedMinutes: { type: integer }
        householdAllocations: { type: number }
    ActiveService:
      type: object
      required: [slug, name, description, dimension, publicPrice, corporatePrice, employerContribution, employeeCopayment, durationMinutes, providerVerification]
      properties:
        slug: { type: string }
        name: { type: string }
        description: { type: string }
        dimension: { type: string }
        publicPrice: { type: number }
        corporatePrice: { type: number }
        employerContribution: { type: number }
        employeeCopayment: { type: number }
        durationMinutes: { type: integer }
        providerVerification: { type: string }
    Routine:
      type: object
      required: [id, label, categorySlug, frequency, preferredDay, preferredTime, maxCopayment, manualConfirmation, status]
      properties:
        id: { type: integer }
        label: { type: string }
        categorySlug: { type: string }
        frequency: { type: string }
        preferredDay: { type: string }
        preferredTime: { type: string }
        maxCopayment: { type: number }
        manualConfirmation: { type: boolean }
        status: { type: string }
    EmployerOverview:
      type: object
      required:
        - employerName
        - eligibleEmployees
        - activatedEmployees
        - authorizedMaximum
        - redeemedAllowances
        - reservedAllowances
        - forecastRedemptions
        - invoiceEstimate
        - completionRate
        - satisfaction
      properties:
        employerName: { type: string }
        eligibleEmployees: { type: integer }
        activatedEmployees: { type: integer }
        authorizedMaximum: { type: number }
        redeemedAllowances: { type: number }
        reservedAllowances: { type: number }
        forecastRedemptions: { type: number }
        invoiceEstimate: { type: number }
        completionRate: { type: number }
        satisfaction: { type: number }
    EmployerEmployee:
      type: object
      required: [id, externalEmployeeId, name, workEmail, department, benefitTier, eligibilityStatus, householdEligible]
      properties:
        id: { type: integer }
        externalEmployeeId: { type: string }
        name: { type: string }
        workEmail: { type: string }
        department: { type: string }
        benefitTier: { type: string }
        eligibilityStatus: { type: string }
        householdEligible: { type: boolean }
    EmployeeImportInput:
      type: object
      required: [csv]
      properties:
        csv:
          type: string
          minLength: 1
    ImportResult:
      type: object
      required: [status, imported, skipped, message]
      properties:
        status: { type: string }
        imported: { type: integer }
        skipped: { type: integer }
        message: { type: string }
    UtilizationReport:
      type: object
      required: [activationRate, redemptionRate, repeatUsageRate, averageSupportPerActiveEmployee, categoryUtilization, corporateSavings, estimatedTimeSavedMinutes, satisfaction, completionRate, serviceRecoveryRate]
      properties:
        activationRate: { type: number }
        redemptionRate: { type: number }
        repeatUsageRate: { type: number }
        averageSupportPerActiveEmployee: { type: number }
        categoryUtilization:
          type: array
          items:
            $ref: "#/components/schemas/CategoryUtilization"
        corporateSavings: { type: number }
        estimatedTimeSavedMinutes: { type: integer }
        satisfaction: { type: number }
        completionRate: { type: number }
        serviceRecoveryRate: { type: number }
    CategoryUtilization:
      type: object
      required: [category, bookings, share]
      properties:
        category: { type: string }
        bookings: { type: integer }
        share: { type: number }
    IntegrationDetails:
      type: object
      required: [ssoLabel, ssoUrl, widgetScript, widgetSnippet, apiMode]
      properties:
        ssoLabel: { type: string }
        ssoUrl: { type: string }
        widgetScript: { type: string }
        widgetSnippet: { type: string }
        apiMode: { type: string }
    VendorToday:
      type: object
      required: [vendorName, assignedBookings, awaitingAcceptance, availableCapacity, capacityWarnings, lateArrivalWarnings, serviceRecoveryActions]
      properties:
        vendorName: { type: string }
        assignedBookings:
          type: array
          items:
            $ref: "#/components/schemas/VendorBooking"
        awaitingAcceptance: { type: integer }
        availableCapacity: { type: integer }
        capacityWarnings: { type: integer }
        lateArrivalWarnings: { type: integer }
        serviceRecoveryActions: { type: integer }
    VendorBooking:
      type: object
      required: [id, category, zone, scheduledAt, status, operationalRequirement]
      properties:
        id: { type: integer }
        category: { type: string }
        zone: { type: string }
        scheduledAt: { type: string, format: date-time }
        status: { type: string }
        operationalRequirement: { type: string }
    ForecastReport:
      type: object
      required: [updatedAt, confidence, windows]
      properties:
        updatedAt: { type: string, format: date-time }
        confidence: { type: number }
        windows:
          type: array
          items:
            $ref: "#/components/schemas/ForecastWindow"
    ForecastWindow:
      type: object
      required: [label, category, zone, expectedBookings, low, high, capacityGap, recommendation]
      properties:
        label: { type: string }
        category: { type: string }
        zone: { type: string }
        expectedBookings: { type: integer }
        low: { type: integer }
        high: { type: integer }
        capacityGap: { type: integer }
        recommendation: { type: string }
    VendorPerformance:
      type: object
      required: [vendorName, status, completionRate, onTimeRate, cancellationRate, averageRating, complaintRate, serviceRecoveryRate, capacityAccuracy]
      properties:
        vendorName: { type: string }
        status: { type: string }
        completionRate: { type: number }
        onTimeRate: { type: number }
        cancellationRate: { type: number }
        averageRating: { type: number }
        complaintRate: { type: number }
        serviceRecoveryRate: { type: number }
        capacityAccuracy: { type: number }
    OperationsOverview:
      type: object
      required: [forecastBookings, confirmedBookings, unfulfilledDemand, capacityShortages, highRiskBookings, providerIncidents, transactionVolume, spendingAlerts, matching, methodology]
      properties:
        forecastBookings: { type: integer }
        confirmedBookings: { type: integer }
        unfulfilledDemand: { type: integer }
        capacityShortages: { type: integer }
        highRiskBookings: { type: integer }
        providerIncidents: { type: integer }
        transactionVolume: { type: number }
        spendingAlerts: { type: integer }
        matching:
          type: array
          items:
            $ref: "#/components/schemas/MatchDecision"
        methodology: { type: string }
    MatchDecision:
      type: object
      required: [bookingId, category, zone, selectedProvider, score, drivers, manualOverride]
      properties:
        bookingId: { type: integer }
        category: { type: string }
        zone: { type: string }
        selectedProvider: { type: string }
        score: { type: number }
        drivers:
          type: array
          items: { type: string }
        manualOverride: { type: boolean }
    ServiceFitEvaluation:
      type: object
      required: [service, status, dimensions, problemFrequency, timeSaved, employerRelevance, repeatPotential, standardization, providerCoverage, regulatoryComplexity, overallFit]
      properties:
        service: { type: string }
        status: { type: string }
        dimensions:
          type: array
          items: { type: string }
        problemFrequency: { type: integer }
        timeSaved: { type: integer }
        employerRelevance: { type: integer }
        repeatPotential: { type: integer }
        standardization: { type: integer }
        providerCoverage: { type: integer }
        regulatoryComplexity: { type: integer }
        overallFit: { type: integer }
    AuditEvent:
      type: object
      required: [id, actorRole, action, entityType, entityId, createdAt]
      properties:
        id: { type: integer }
        actorRole: { type: string }
        action: { type: string }
        entityType: { type: string }
        entityId: { type: string }
        createdAt: { type: string, format: date-time }
    PushPublicKey:
      type: object
      required: [publicKey]
      properties:
        publicKey:
          type: string
    PushSubscriptionInput:
      type: object
      required: [endpoint, keys]
      properties:
        endpoint:
          type: string
          minLength: 1
        keys:
          type: object
          required: [p256dh, auth]
          properties:
            p256dh:
              type: string
              minLength: 1
            auth:
              type: string
              minLength: 1
    PushUnsubscribeInput:
      type: object
      required: [endpoint]
      properties:
        endpoint:
          type: string
          minLength: 1
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ApiMessage:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    HomeSummary:
      type: object
      properties:
        memberName:
          type: string
        householdName:
          type: string
        activeBooking:
          oneOf:
            - $ref: "#/components/schemas/Booking"
            - type: "null"
        nextBooking:
          oneOf:
            - $ref: "#/components/schemas/Booking"
            - type: "null"
        upcomingCount:
          type: integer
        openBillTotal:
          type: number
        monthToDateSpend:
          type: number
        memberCount:
          type: integer
        isHeadOfHousehold:
          type: boolean
          description: Whether the current member can approve or decline service requests
        packUnreadCount:
          type: integer
        recentPackMessages:
          type: array
          description: Latest household thread messages, newest first
          items:
            $ref: "#/components/schemas/PackMessage"
        pendingRequests:
          type: array
          description: Pending service requests awaiting a decision
          items:
            $ref: "#/components/schemas/ServiceRequest"
      required:
        - memberName
        - householdName
        - activeBooking
        - nextBooking
        - upcomingCount
        - openBillTotal
        - monthToDateSpend
        - memberCount
        - isHeadOfHousehold
        - packUnreadCount
        - recentPackMessages
        - pendingRequests
    Category:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        tagline:
          type: string
        icon:
          type: string
          description: Lucide icon name, e.g. Sparkles, AirVent, Wrench
        providerCount:
          type: integer
        startingPrice:
          type: number
      required:
        - id
        - name
        - slug
        - tagline
        - icon
        - providerCount
        - startingPrice
    Provider:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        categorySlug:
          type: string
        categoryName:
          type: string
        tagline:
          type: string
        bio:
          type: string
        rating:
          type: number
        reviewCount:
          type: integer
        jobsCompleted:
          type: integer
        yearsOnPlatform:
          type: integer
        verified:
          type: boolean
        availableNow:
          type: boolean
        responseMinutes:
          type: integer
          description: Typical response time in minutes
        startingPrice:
          type: number
        badges:
          type: array
          items:
            type: string
      required:
        - id
        - name
        - categorySlug
        - categoryName
        - tagline
        - bio
        - rating
        - reviewCount
        - jobsCompleted
        - yearsOnPlatform
        - verified
        - availableNow
        - responseMinutes
        - startingPrice
        - badges
    Service:
      type: object
      properties:
        id:
          type: integer
        providerId:
          type: integer
        name:
          type: string
        description:
          type: string
        price:
          type: number
        durationMinutes:
          type: integer
      required:
        - id
        - providerId
        - name
        - description
        - price
        - durationMinutes
    ProviderDetail:
      allOf:
        - $ref: "#/components/schemas/Provider"
        - type: object
          properties:
            services:
              type: array
              items:
                $ref: "#/components/schemas/Service"
          required:
            - services
    Review:
      type: object
      properties:
        id:
          type: integer
        providerId:
          type: integer
        authorName:
          type: string
        rating:
          type: integer
        comment:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - providerId
        - authorName
        - rating
        - comment
        - createdAt
    ReviewInput:
      type: object
      properties:
        bookingId:
          type: integer
        rating:
          type: integer
          minimum: 1
          maximum: 5
        comment:
          type: string
      required:
        - bookingId
        - rating
        - comment
    Household:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        members:
          type: array
          items:
            $ref: "#/components/schemas/HouseholdMember"
      required:
        - id
        - name
        - members
    HouseholdMember:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        relation:
          type: string
          description: e.g. Head of Household, Partner, Son, Housekeeper
        role:
          type: string
          enum: [head, owner, member]
        initials:
          type: string
        monthlySpendLimit:
          type: ["number", "null"]
        monthToDateSpend:
          type: number
        isCurrentUser:
          type: boolean
      required:
        - id
        - name
        - relation
        - role
        - initials
        - monthlySpendLimit
        - monthToDateSpend
        - isCurrentUser
    ActivityItem:
      type: object
      properties:
        id:
          type: integer
        kind:
          type: string
          enum: [booking_created, booking_completed, payment, review]
        memberName:
          type: string
        description:
          type: string
        amount:
          type: ["number", "null"]
        occurredAt:
          type: string
          format: date-time
      required:
        - id
        - kind
        - memberName
        - description
        - amount
        - occurredAt
    Address:
      type: object
      properties:
        id:
          type: integer
        label:
          type: string
        area:
          type: string
          description: Dubai neighbourhood, e.g. Jumeirah, Downtown
        street:
          type: string
        instructions:
          type: ["string", "null"]
      required:
        - id
        - label
        - area
        - street
        - instructions
    Booking:
      type: object
      properties:
        id:
          type: integer
        providerId:
          type: integer
        providerName:
          type: string
        serviceId:
          type: integer
        serviceName:
          type: string
        categoryName:
          type: string
        categoryIcon:
          type: string
        memberId:
          type: integer
        memberName:
          type: string
        addressId:
          type: integer
        addressLabel:
          type: string
        scheduledAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/BookingStatus"
        priceEstimate:
          type: number
        instructions:
          type: ["string", "null"]
        etaMinutes:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - providerId
        - providerName
        - serviceId
        - serviceName
        - categoryName
        - categoryIcon
        - memberId
        - memberName
        - addressId
        - addressLabel
        - scheduledAt
        - status
        - priceEstimate
        - instructions
        - etaMinutes
        - createdAt
    BookingStatus:
      type: string
      enum:
        - pending
        - confirmed
        - en_route
        - arrived
        - in_progress
        - completed
        - cancelled
    BookingEvent:
      type: object
      properties:
        id:
          type: integer
        bookingId:
          type: integer
        status:
          $ref: "#/components/schemas/BookingStatus"
        note:
          type: string
        occurredAt:
          type: string
          format: date-time
      required:
        - id
        - bookingId
        - status
        - note
        - occurredAt
    BookingDetail:
      allOf:
        - $ref: "#/components/schemas/Booking"
        - type: object
          properties:
            events:
              type: array
              items:
                $ref: "#/components/schemas/BookingEvent"
            hasReview:
              type: boolean
          required:
            - events
            - hasReview
    BookingInput:
      type: object
      properties:
        serviceId:
          type: integer
        addressId:
          type: integer
        scheduledAt:
          type: string
          format: date-time
        instructions:
          type: string
      required:
        - serviceId
        - addressId
        - scheduledAt
    BookingUpdate:
      type: object
      properties:
        scheduledAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [cancelled]
    Message:
      type: object
      properties:
        id:
          type: integer
        bookingId:
          type: integer
        sender:
          type: string
          enum: [member, provider]
        senderName:
          type: string
        body:
          type: string
        sentAt:
          type: string
          format: date-time
      required:
        - id
        - bookingId
        - sender
        - senderName
        - body
        - sentAt
    MessageInput:
      type: object
      properties:
        body:
          type: string
          minLength: 1
      required:
        - body
    PackMessage:
      type: object
      properties:
        id:
          type: integer
        memberId:
          type: integer
        memberName:
          type: string
        initials:
          type: string
        isCurrentUser:
          type: boolean
        body:
          type: string
        sentAt:
          type: string
          format: date-time
      required:
        - id
        - memberId
        - memberName
        - initials
        - isCurrentUser
        - body
        - sentAt
    PackMessageInput:
      type: object
      properties:
        body:
          type: string
          minLength: 1
      required:
        - body
    ServiceRequest:
      type: object
      properties:
        id:
          type: integer
        memberId:
          type: integer
        memberName:
          type: string
        initials:
          type: string
        serviceId:
          type: integer
        serviceName:
          type: string
        providerName:
          type: string
        categoryName:
          type: string
        categoryIcon:
          type: string
        price:
          type: number
        note:
          type: string
        status:
          type: string
          enum: [pending, approved, declined]
        bookingId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        decidedAt:
          type: ["string", "null"]
          format: date-time
      required:
        - id
        - memberId
        - memberName
        - initials
        - serviceId
        - serviceName
        - providerName
        - categoryName
        - categoryIcon
        - price
        - note
        - status
        - bookingId
        - createdAt
        - decidedAt
    ServiceRequestInput:
      type: object
      properties:
        serviceId:
          type: integer
        note:
          type: string
          minLength: 1
      required:
        - serviceId
        - note
    PaymentMethod:
      type: object
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [card, wallet, cash]
        label:
          type: string
        detail:
          type: ["string", "null"]
          description: e.g. "Visa ending 4421" or wallet balance text
        isDefault:
          type: boolean
      required:
        - id
        - type
        - label
        - detail
        - isDefault
    BillItem:
      type: object
      properties:
        id:
          type: integer
        bookingId:
          type: integer
        serviceName:
          type: string
        providerName:
          type: string
        memberName:
          type: string
        categoryName:
          type: string
        amount:
          type: number
        date:
          type: string
          format: date-time
      required:
        - id
        - bookingId
        - serviceName
        - providerName
        - memberName
        - categoryName
        - amount
        - date
    MemberSpend:
      type: object
      properties:
        memberId:
          type: integer
        memberName:
          type: string
        initials:
          type: string
        amount:
          type: number
      required:
        - memberId
        - memberName
        - initials
        - amount
    CategorySpend:
      type: object
      properties:
        categoryName:
          type: string
        amount:
          type: number
      required:
        - categoryName
        - amount
    BillStatement:
      type: object
      properties:
        id:
          type: integer
        month:
          type: string
          description: Human label, e.g. "August 2026"
        status:
          type: string
          enum: [open, paid]
        total:
          type: number
        paidAt:
          type: ["string", "null"]
          format: date-time
        paidWith:
          type: ["string", "null"]
        items:
          type: array
          items:
            $ref: "#/components/schemas/BillItem"
        byMember:
          type: array
          items:
            $ref: "#/components/schemas/MemberSpend"
        byCategory:
          type: array
          items:
            $ref: "#/components/schemas/CategorySpend"
      required:
        - id
        - month
        - status
        - total
        - paidAt
        - paidWith
        - items
        - byMember
        - byCategory
    BillStatementSummary:
      type: object
      properties:
        id:
          type: integer
        month:
          type: string
        status:
          type: string
          enum: [open, paid]
        total:
          type: number
        itemCount:
          type: integer
        paidAt:
          type: ["string", "null"]
          format: date-time
        paidWith:
          type: ["string", "null"]
      required:
        - id
        - month
        - status
        - total
        - itemCount
        - paidAt
        - paidWith
    PaymentInput:
      type: object
      properties:
        paymentMethodId:
          type: integer
      required:
        - paymentMethodId
    OpenaiConversation:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - title
        - createdAt
    OpenaiMessage:
      type: object
      properties:
        id:
          type: integer
        conversationId:
          type: integer
        role:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - conversationId
        - role
        - content
        - createdAt
    OpenaiConversationInput:
      type: object
      properties:
        title:
          type: string
      required:
        - title
    OpenaiMessageInput:
      type: object
      properties:
        content:
          type: string
      required:
        - content
    OpenaiConversationWithMessages:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        createdAt:
          type: string
          format: date-time
        messages:
          type: array
          items:
            $ref: "#/components/schemas/OpenaiMessage"
      required:
        - id
        - title
        - createdAt
        - messages
    OpenaiError:
      type: object
      properties:
        error:
          type: string
      required:
        - error
