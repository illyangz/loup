import { useState } from "react";
import { ArrowRight, CalendarDays, Check, ChevronRight, Clock3, ExternalLink, Home, Sparkles } from "lucide-react";
import { Link } from "wouter";
import { useGetEmployeeOverview, getGetEmployeeOverviewQueryKey } from "@workspace/api-client-react";
import { DataState } from "@/components/platform-shell";

function money(value: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "AED", maximumFractionDigits: 0 }).format(value); }
function date(value: string) { return new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric" }).format(new Date(value)); }

export default function EmbedDemo() {
  const [booked, setBooked] = useState(false);
  const query = useGetEmployeeOverview({ query: { queryKey: getGetEmployeeOverviewQueryKey() } });
  const data = query.data;
  return <div className="min-h-[100dvh] bg-[#e9dfcf] px-4 py-8 sm:px-10 platform-grid"><header className="mx-auto flex max-w-5xl items-center justify-between"><Link href="/login" className="flex items-center gap-2" data-testid="link-embed-brand"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-foreground font-serif text-lg italic text-background">L</span><span className="font-serif text-2xl italic">Loup</span></Link><span className="rounded-full border border-foreground/15 bg-background/40 px-3 py-1 text-[11px] uppercase tracking-[0.14em] text-muted-foreground">Simulated embed preview</span></header><main className="mx-auto grid max-w-5xl items-center gap-10 py-12 lg:grid-cols-[.8fr_1.2fr] lg:py-20"><div className="platform-reveal"><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-primary">Employee benefit widget</p><h1 className="mt-4 text-5xl leading-[.9] sm:text-6xl">A small doorway to a lighter day.</h1><p className="mt-5 max-w-sm text-sm leading-6 text-muted-foreground">A focused, embeddable employee experience for a benefits hub, intranet, or HR app.</p><div className="mt-8 flex items-center gap-2 text-xs text-muted-foreground"><span className="h-2 w-2 rounded-full bg-primary" /> Designed to feel native to the place it lives</div></div><div className="rounded-[28px] border border-foreground/10 bg-background p-5 shadow-[0_25px_80px_rgba(76,52,32,.16)] sm:p-7" data-testid="card-embed-widget"><div className="flex items-center justify-between border-b border-border/70 pb-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-foreground font-serif italic text-background">L</div><div><p className="text-sm font-medium">Your Loup benefit</p><p className="text-xs text-muted-foreground">A little more room in the week</p></div></div><Sparkles className="h-4 w-4 text-primary" /></div><DataState loading={query.isLoading} error={query.isError} onRetry={() => void query.refetch()}>{data && <div className="pt-5"><div className="rounded-2xl bg-foreground p-5 text-background"><div className="flex items-start justify-between"><div><p className="text-xs text-background/60">Available allowance</p><p className="mt-1 font-serif text-4xl">{money(data.allowance.available)}</p></div><p className="text-right text-[11px] text-background/60">Renews<br /><span className="text-background">{date(data.allowance.renewalDate)}</span></p></div><div className="mt-4 h-1.5 rounded-full bg-background/15"><div className="h-full rounded-full bg-primary" style={{ width: `${(data.allowance.redeemed / data.allowance.authorized) * 100}%` }} /></div></div><div className="mt-6"><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-[0.12em] text-muted-foreground">Next up</p><p className="mt-1 text-sm font-medium">{booked ? "Request received" : data.upcomingBooking?.serviceName ?? "Your next service"}</p></div><CalendarDays className="h-4 w-4 text-primary" /></div>{booked ? <div className="mt-3 flex items-center gap-2 rounded-xl bg-primary/10 p-3 text-sm text-primary" data-testid="status-embed-booked"><Check className="h-4 w-4" /> A Loup coordinator will confirm your time.</div> : data.upcomingBooking ? <div className="mt-3 flex items-center gap-3 rounded-xl border border-border/70 p-3"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-accent"><Clock3 className="h-4 w-4 text-primary" /></div><div><p className="text-xs">{date(data.upcomingBooking.scheduledAt)} · {new Date(data.upcomingBooking.scheduledAt).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}</p><p className="text-xs text-muted-foreground">{data.upcomingBooking.providerName}</p></div><ChevronRight className="ml-auto h-4 w-4 text-muted-foreground" /></div> : <p className="mt-3 text-xs text-muted-foreground">No booking on the calendar yet.</p>}</div><div className="mt-6"><p className="text-xs uppercase tracking-[0.12em] text-muted-foreground">Eligible services</p><div className="mt-3 flex flex-wrap gap-2">{data.activeCategories.slice(0, 4).map((service) => <span key={service.slug} className="inline-flex items-center gap-1.5 rounded-full bg-accent px-3 py-1.5 text-xs" data-testid={`chip-embed-service-${service.slug}`}><Home className="h-3 w-3 text-primary" />{service.name}</span>)}</div></div><button type="button" onClick={() => setBooked(true)} disabled={booked} className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3 text-sm font-medium text-primary-foreground transition-transform hover:-translate-y-0.5 disabled:cursor-default disabled:opacity-70" data-testid="button-embed-book">{booked ? "Request received" : "Book a service"} {!booked && <ArrowRight className="h-4 w-4" />}</button></div>}</DataState><div className="mt-5 flex items-center justify-center gap-1.5 text-[11px] text-muted-foreground"><ExternalLink className="h-3 w-3" /> Opens in the full Loup experience</div></div></main></div>;
}